# Changesets

Changesets track package version intent once package source exists.

Phase 00 only adds versioning metadata. Publishing workflows and provenance checks
are intentionally deferred until package entry points and npm ownership are real.
