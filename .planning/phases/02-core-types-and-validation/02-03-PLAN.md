---
phase: 02-core-types-and-validation
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified: ["packages/boxnow-core/src/primitives.ts", "packages/boxnow-core/src/locker.ts", "packages/boxnow-core/src/index.ts", "packages/boxnow-core/src/primitives.test.ts", "packages/boxnow-core/src/locker.test.ts"]
autonomous: true
requirements: ["02-02", "02-03", "02-04", "02-05", "02-09", "02-10"]
user_setup: []
must_haves:
  truths:
    - "Environment, country, compartment-size, locker, origin, destination, and LockerSnapshot contracts exist as pure types/schemas without HTTP behavior."
    - "LockerSnapshot stays host-facing and ergonomic while raw widget protocol fields are validated in mapper/schema code."
    - "D-02-19 through D-02-21 are respected for widget/domain contracts."
  artifacts:
    - path: "packages/boxnow-core/src/primitives.ts"
      provides: "Shared primitive schemas and types"
      min_lines: 30
    - path: "packages/boxnow-core/src/locker.ts"
      provides: "Locker, Origin, Destination, and LockerSnapshot contracts"
      min_lines: 40
    - path: "packages/boxnow-core/src/locker.test.ts"
      provides: "Locker and LockerSnapshot tests"
      min_lines: 30
  key_links:
    - from: "packages/boxnow-core/src/locker.ts"
      to: "packages/boxnow-core/src/primitives.ts"
      via: "shared primitive schema imports"
      pattern: "CompartmentSize|CountryCode|Locker"
---

<objective>
Implement shared primitives and locker/location validation contracts.

Purpose: Establish the browser-safe and shared location model layer before server delivery-request protocol types.
Output: Environment/country/compartment primitives, locker/origin/destination models, LockerSnapshot normalization contracts, and tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/UBIQUITOUS_LANGUAGE.md
@.planning/public-api-sketch.md
@.planning/research/boxnow-widget.md
@.planning/research/phase-02-external-library-audit.md
@.planning/phases/02-core-types-and-validation/02-CONTEXT.md
@.planning/phases/02-core-types-and-validation/02-RESEARCH.md
@.planning/phases/02-core-types-and-validation/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add primitive schemas and types</name>
  <files>packages/boxnow-core/src/primitives.ts, packages/boxnow-core/src/index.ts</files>
  <read_first>packages/boxnow-core/src/validation.ts, .planning/UBIQUITOUS_LANGUAGE.md, .planning/public-api-sketch.md</read_first>
  <action>Create `packages/boxnow-core/src/primitives.ts` with Valibot schemas and inferred types for `BoxNowEnvironment`, `BoxNowCountryCode`, `CompartmentSize`, and small ID/string primitives needed by locker and protocol types. Use simple literal unions where values are known. Do not include server credentials here. Export these from `src/index.ts`.</action>
  <verify>C:\Users\SVall\.local\bin\rtk.exe rg -n "BoxNowEnvironment|BoxNowCountryCode|CompartmentSize|InferOutput|valibot" packages/boxnow-core/src/primitives.ts packages/boxnow-core/src/index.ts</verify>
  <acceptance_criteria>
    - `BoxNowEnvironment` type/schema exists.
    - `BoxNowCountryCode` type/schema exists.
    - `CompartmentSize` type/schema exists.
    - No credential fields appear in `primitives.ts`.
  </acceptance_criteria>
  <done>Core primitives are available for later schemas.</done>
</task>

<task type="auto">
  <name>Task 2: Add locker, origin, destination, and LockerSnapshot contracts</name>
  <files>packages/boxnow-core/src/locker.ts, packages/boxnow-core/src/index.ts</files>
  <read_first>packages/boxnow-core/src/primitives.ts, .planning/research/boxnow-widget.md, .planning/research/phase-02-external-library-audit.md, .planning/adrs/ADR-003-widget-contract.md</read_first>
  <action>Create `packages/boxnow-core/src/locker.ts` with Valibot schemas and inferred types for `Locker`, `Origin`, `Destination`, `LockerSnapshot`, and raw widget selection input. Make `LockerSnapshot` host-facing with fields such as `id`, `name`, `addressLine1`, `postalCode`, and optional `country`. Add a `normalizeLockerSelection(value: unknown)` helper that validates raw widget selection fields such as `boxnowLockerId`, `boxnowLockerName`, `boxnowLockerAddressLine1`, and `boxnowLockerPostalCode`, then returns `BoxNowValidationResult<LockerSnapshot>`. Export from `src/index.ts`.</action>
  <verify>C:\Users\SVall\.local\bin\rtk.exe rg -n "LockerSnapshot|normalizeLockerSelection|boxnowLockerId|addressLine1|postalCode" packages/boxnow-core/src/locker.ts packages/boxnow-core/src/index.ts</verify>
  <acceptance_criteria>
    - Raw widget protocol fields are accepted only by mapper/schema code.
    - `LockerSnapshot` output uses host-facing fields, not raw `boxnowLocker*` names.
    - `normalizeLockerSelection` returns `BoxNowValidationResult<LockerSnapshot>`.
  </acceptance_criteria>
  <done>Locker and widget-selection contracts are implemented without hidden storage authority.</done>
</task>

<task type="auto">
  <name>Task 3: Add tests for primitives and locker normalization</name>
  <files>packages/boxnow-core/src/primitives.test.ts, packages/boxnow-core/src/locker.test.ts</files>
  <read_first>packages/boxnow-core/src/primitives.ts, packages/boxnow-core/src/locker.ts, .planning/research/boxnow-widget.md</read_first>
  <action>Add Vitest coverage for known country/environment/compartment values, valid widget selection normalization, invalid widget payload rejection, and confirmation that `LockerSnapshot` output does not include raw `boxnowLockerId` fields. Use no browser APIs and no localStorage.</action>
  <verify>pnpm exec vitest run packages/boxnow-core/src/primitives.test.ts packages/boxnow-core/src/locker.test.ts</verify>
  <acceptance_criteria>
    - Tests pass for valid primitives.
    - Invalid widget payload returns issues.
    - Tests assert no hidden localStorage/session behavior exists.
  </acceptance_criteria>
  <done>Primitives and locker contracts are covered by focused unit tests.</done>
</task>

</tasks>

<verification>
- [ ] `pnpm check`
- [ ] `pnpm test`
- [ ] `pnpm build`
</verification>

<success_criteria>
- Shared primitive and locker/domain contracts exist and are pure.
- Widget-facing normalization is host-facing, non-throwing, and tested.
- No browser persistence or server credential behavior appears in the core contracts.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-types-and-validation/02-03-SUMMARY.md`
</output>
