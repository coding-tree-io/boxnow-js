---
phase: 02-core-types-and-validation
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: ["package.json", "pnpm-lock.yaml", "tsdown.config.ts", "packages/boxnow-core/package.json", "packages/boxnow-core/README.md", "packages/boxnow-core/src/index.ts", "packages/boxnow-core/src/validation.ts", "packages/boxnow-core/src/validation.test.ts"]
autonomous: true
requirements: ["02-01", "02-09", "02-10"]
user_setup: []
must_haves:
  truths:
    - "D-02-08 through D-02-18 are translated into an internal shared workspace package with Valibot-backed Standard Schema-compatible validation."
    - "The internal package is not documented as an end-user import."
    - "Primary validation APIs return result objects and Phase 02 introduces no throwing wrappers."
  artifacts:
    - path: "packages/boxnow-core/package.json"
      provides: "Internal shared package metadata and dependencies"
      contains: "\"private\": true"
    - path: "packages/boxnow-core/src/validation.ts"
      provides: "Shared Standard Schema-compatible validation helper layer"
      min_lines: 30
    - path: "packages/boxnow-core/src/validation.test.ts"
      provides: "Validation foundation tests"
      min_lines: 20
  key_links:
    - from: "packages/boxnow-core/src/validation.ts"
      to: "valibot"
      via: "internal schema implementation"
      pattern: "valibot"
    - from: "packages/boxnow-core/src/validation.ts"
      to: "@standard-schema/spec"
      via: "public schema interface typing"
      pattern: "StandardSchemaV1"
---

<objective>
Create the internal shared validation package foundation.

Purpose: Give later Phase 02 plans a single internal place for shared models, schemas, and non-throwing validation helpers.
Output: `packages/boxnow-core` with package metadata, README boundary, Valibot/Standard Schema dependencies, validation helper exports, and tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-types-and-validation/02-CONTEXT.md
@.planning/phases/02-core-types-and-validation/02-RESEARCH.md
@.planning/phases/02-core-types-and-validation/02-01-SUMMARY.md
@package.json
@pnpm-workspace.yaml
@tsdown.config.ts
@vitest.config.ts
@tsconfig.base.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add internal package metadata and dependencies</name>
  <files>package.json, pnpm-lock.yaml, packages/boxnow-core/package.json, packages/boxnow-core/README.md</files>
  <read_first>package.json, pnpm-workspace.yaml, .planning/phases/02-core-types-and-validation/02-CONTEXT.md, .planning/phases/02-core-types-and-validation/02-RESEARCH.md</read_first>
  <action>Create `packages/boxnow-core/package.json` with name `@coding-tree-io/boxnow-core`, version `0.0.0`, `private: true`, `type: module`, package exports for `./src/index.ts` during source-phase development, and dependencies on `valibot` and `@standard-schema/spec`. Use pnpm to add dependencies so `pnpm-lock.yaml` updates normally. Create `packages/boxnow-core/README.md` stating this package is internal and not the supported end-user import path.</action>
  <verify>C:\Users\SVall\.local\bin\rtk.exe rg -n "\"@coding-tree-io/boxnow-core\"|\"private\": true|valibot|@standard-schema/spec|internal" packages/boxnow-core/package.json packages/boxnow-core/README.md pnpm-lock.yaml</verify>
  <acceptance_criteria>
    - `packages/boxnow-core/package.json` has `"private": true`.
    - `packages/boxnow-core/package.json` depends on `valibot`.
    - `packages/boxnow-core/package.json` depends on `@standard-schema/spec`.
    - README says the package is internal and not the supported end-user import path.
  </acceptance_criteria>
  <done>Internal package metadata and dependency lockfile entries exist.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the non-throwing validation foundation</name>
  <files>packages/boxnow-core/src/index.ts, packages/boxnow-core/src/validation.ts</files>
  <read_first>packages/boxnow-core/package.json, .planning/phases/02-core-types-and-validation/02-CONTEXT.md, .planning/phases/02-core-types-and-validation/02-RESEARCH.md</read_first>
  <action>Create `packages/boxnow-core/src/validation.ts` with exported `BoxNowValidationResult<T>` as a Standard Schema-shaped result union: success has `value` and no `issues`; failure has `issues` and no `value`. Import `StandardSchemaV1` from `@standard-schema/spec`. Add a generic `validateWithSchema<TInput, TOutput>(schema: StandardSchemaV1<TInput, TOutput>, value: unknown): Promise<BoxNowValidationResult<TOutput>> | BoxNowValidationResult<TOutput>` that calls `schema["~standard"].validate(value)`. Preserve async support. Do not add throwing wrappers. Export validation APIs from `src/index.ts`.</action>
  <verify>C:\Users\SVall\.local\bin\rtk.exe rg -n "BoxNowValidationResult|StandardSchemaV1|~standard|validateWithSchema|issues|value" packages/boxnow-core/src/validation.ts packages/boxnow-core/src/index.ts</verify>
  <acceptance_criteria>
    - `validateWithSchema` uses `schema["~standard"].validate`.
    - Validation result does not expose Valibot-native issue types.
    - No function named `parseOrThrow`, `assertValid`, or similar throwing wrapper is added.
  </acceptance_criteria>
  <done>Shared validation APIs are Standard Schema-compatible and non-throwing.</done>
</task>

<task type="auto">
  <name>Task 3: Test the validation foundation</name>
  <files>packages/boxnow-core/src/validation.test.ts</files>
  <read_first>packages/boxnow-core/src/validation.ts, vitest.config.ts</read_first>
  <action>Create Vitest tests using Valibot schemas to prove `validateWithSchema` returns a success result with `value` for valid input and a failure result with `issues` for invalid input. Include a test that confirms callers can branch on `issues` without catching exceptions.</action>
  <verify>pnpm exec vitest run packages/boxnow-core/src/validation.test.ts</verify>
  <acceptance_criteria>
    - Tests cover valid and invalid input.
    - Tests do not use try/catch to validate normal invalid input.
    - `pnpm exec vitest run packages/boxnow-core/src/validation.test.ts` passes.
  </acceptance_criteria>
  <done>The validation foundation is tested through Valibot-backed Standard Schema compatibility.</done>
</task>

</tasks>

<verification>
- [ ] `pnpm install`
- [ ] `pnpm check`
- [ ] `pnpm test`
- [ ] `pnpm build`
</verification>

<success_criteria>
- `packages/boxnow-core` exists as a private internal workspace package.
- Valibot is used internally and Standard Schema is the public schema-shaped surface.
- No throwing validation wrappers exist in Phase 02 foundation code.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-types-and-validation/02-02-SUMMARY.md`
</output>
