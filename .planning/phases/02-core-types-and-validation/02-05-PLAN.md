---
phase: 02-core-types-and-validation
plan: 05
type: execute
wave: 5
depends_on: ["02-04"]
files_modified: ["tsdown.config.ts", "packages/boxnow/package.json", "packages/boxnow/src/index.ts", "packages/boxnow/src/index.test.ts", "packages/boxnow-widget/package.json", "packages/boxnow-widget/src/index.ts", "packages/boxnow-widget/src/index.test.ts", "packages/boxnow/README.md", "packages/boxnow-widget/README.md"]
autonomous: true
requirements: ["02-01", "02-04", "02-05", "02-06", "02-07", "02-08", "02-09", "02-10"]
user_setup: []
must_haves:
  truths:
    - "Public packages curate re-exports from the internal package without making the internal package the documented user import path."
    - "`@coding-tree-io/boxnow` exposes server/core type contracts without browser/widget authority."
    - "`@coding-tree-io/boxnow-widget` exposes LockerSnapshot and selection normalization contracts without server credentials."
    - "Build/type/test gates prove the new package surfaces compile."
  artifacts:
    - path: "packages/boxnow/package.json"
      provides: "Server package metadata and source exports"
      contains: "\"@coding-tree-io/boxnow\""
    - path: "packages/boxnow/src/index.ts"
      provides: "Curated server package re-exports"
      min_lines: 10
    - path: "packages/boxnow-widget/src/index.ts"
      provides: "Curated widget package re-exports"
      min_lines: 10
    - path: "tsdown.config.ts"
      provides: "Build entries for implemented package entry points"
  key_links:
    - from: "packages/boxnow/src/index.ts"
      to: "packages/boxnow-core/src/index.ts"
      via: "curated server re-exports"
      pattern: "boxnow-core"
    - from: "packages/boxnow-widget/src/index.ts"
      to: "packages/boxnow-core/src/index.ts"
      via: "curated widget re-exports"
      pattern: "LockerSnapshot|normalizeLockerSelection"
---

<objective>
Expose curated public Phase 02 type/validation contracts through the public packages.

Purpose: Preserve the user import story while keeping the shared validation foundation internal.
Output: Package metadata, source entry points, build entries, tests, and README updates for the server and widget public contracts.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/public-api-sketch.md
@.planning/adrs/ADR-001-package-topology.md
@.planning/adrs/ADR-002-secret-boundaries.md
@.planning/adrs/ADR-003-widget-contract.md
@.planning/phases/02-core-types-and-validation/02-CONTEXT.md
@.planning/phases/02-core-types-and-validation/02-RESEARCH.md
@.planning/phases/02-core-types-and-validation/02-04-SUMMARY.md
@packages/boxnow/README.md
@packages/boxnow-widget/README.md
@tsdown.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add public package metadata and curated source entry points</name>
  <files>packages/boxnow/package.json, packages/boxnow/src/index.ts, packages/boxnow-widget/package.json, packages/boxnow-widget/src/index.ts</files>
  <read_first>packages/boxnow-core/src/index.ts, packages/boxnow/README.md, packages/boxnow-widget/README.md, .planning/public-api-sketch.md</read_first>
  <action>Create package metadata and source entry points for `@coding-tree-io/boxnow` and `@coding-tree-io/boxnow-widget`. Each package may depend on the internal workspace package during development, but public README text must keep `@coding-tree-io/boxnow-core` internal and undocumented as an end-user import. In `packages/boxnow/src/index.ts`, curate server/core exports such as environment, country, compartment, locker/origin/destination, delivery request, parcel, label format, protocol validators, and validation result types. In `packages/boxnow-widget/src/index.ts`, curate browser-safe exports such as `BoxNowCountryCode`, `LockerSnapshot`, and `normalizeLockerSelection`; do not export server credential or auth-session types from widget.</action>
  <verify>C:\Users\SVall\.local\bin\rtk.exe rg -n "\"@coding-tree-io/boxnow\"|\"@coding-tree-io/boxnow-widget\"|LockerSnapshot|validateDeliveryRequest|AuthSession|clientSecret" packages/boxnow/package.json packages/boxnow/src/index.ts packages/boxnow-widget/package.json packages/boxnow-widget/src/index.ts</verify>
  <acceptance_criteria>
    - `packages/boxnow/package.json` exists and names `@coding-tree-io/boxnow`.
    - `packages/boxnow-widget/package.json` exists and names `@coding-tree-io/boxnow-widget`.
    - Widget entry point does not export auth/session/server credential types.
    - Public entry points do not tell users to import `@coding-tree-io/boxnow-core`.
  </acceptance_criteria>
  <done>Public packages expose curated Phase 02 contracts.</done>
</task>

<task type="auto">
  <name>Task 2: Add build entries and package surface tests</name>
  <files>tsdown.config.ts, packages/boxnow/src/index.test.ts, packages/boxnow-widget/src/index.test.ts</files>
  <read_first>tsdown.config.ts, packages/boxnow/src/index.ts, packages/boxnow-widget/src/index.ts, vitest.config.ts</read_first>
  <action>Update `tsdown.config.ts` to build implemented source entry points for `packages/boxnow-core`, `packages/boxnow`, and `packages/boxnow-widget`. Add package-surface tests proving the server package exports delivery/protocol validators and the widget package exports locker selection normalization. Include a test or static assertion that widget exports do not include server credential/auth exports.</action>
  <verify>pnpm exec vitest run packages/boxnow/src/index.test.ts packages/boxnow-widget/src/index.test.ts</verify>
  <acceptance_criteria>
    - `tsdown.config.ts` contains entries for all implemented Phase 02 packages.
    - Server package surface test imports from `packages/boxnow/src/index.ts`.
    - Widget package surface test imports from `packages/boxnow-widget/src/index.ts`.
    - Widget package test guards the secret boundary.
  </acceptance_criteria>
  <done>Implemented public package surfaces compile and are tested.</done>
</task>

<task type="auto">
  <name>Task 3: Update README package status without overclaiming behavior</name>
  <files>packages/boxnow/README.md, packages/boxnow-widget/README.md</files>
  <read_first>packages/boxnow/README.md, packages/boxnow-widget/README.md, .planning/phases/02-core-types-and-validation/02-CONTEXT.md</read_first>
  <action>Update the server and widget README stubs to say Phase 02 provides pure type/schema/validation contracts only. Keep non-goals: no HTTP client behavior yet, no widget loader behavior yet, no hidden storage, no checkout/payment/order ownership, no server credentials in browser. Mention `@coding-tree-io/boxnow-core` only as an internal implementation detail if needed, not as a supported user import path.</action>
  <verify>C:\Users\SVall\.local\bin\rtk.exe rg -n "pure type|validation|no HTTP|no widget loader|internal implementation|not.*import" packages/boxnow/README.md packages/boxnow-widget/README.md</verify>
  <acceptance_criteria>
    - README files do not claim implemented HTTP or widget-loader behavior.
    - README files preserve secret/storage/checkout boundaries.
    - README files do not promote `@coding-tree-io/boxnow-core` as an end-user package.
  </acceptance_criteria>
  <done>Package docs match the actual Phase 02 surface.</done>
</task>

</tasks>

<verification>
- [ ] `pnpm install`
- [ ] `pnpm check`
- [ ] `pnpm test`
- [ ] `pnpm build`
- [ ] `C:\Users\SVall\.local\bin\rtk.exe pwsh -NoProfile -Command "if (Select-String -Path 'packages/boxnow/README.md','packages/boxnow-widget/README.md' -Pattern 'npm install @coding-tree-io/boxnow-core|from \"@coding-tree-io/boxnow-core\"|from ''@coding-tree-io/boxnow-core''' -Quiet) { exit 1 }"` passes.
</verification>

<success_criteria>
- Public package imports remain package-specific and curated.
- Internal package remains internal.
- Type/schema/validation contracts compile, test, and build without HTTP/widget runtime behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-types-and-validation/02-05-SUMMARY.md`
</output>
