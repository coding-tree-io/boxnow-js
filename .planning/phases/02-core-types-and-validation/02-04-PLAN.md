---
phase: 02-core-types-and-validation
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
files_modified: ["packages/boxnow-core/src/protocol.ts", "packages/boxnow-core/src/index.ts", "packages/boxnow-core/src/protocol.test.ts"]
autonomous: true
requirements: ["02-06", "02-07", "02-08", "02-09", "02-10"]
user_setup: []
must_haves:
  truths:
    - "DeliveryRequest, Parcel, ParcelLabelFormat, auth response, origins, destinations, and parcel protocol types exist as pure schemas/types."
    - "Wire-facing request and response types are protocol-first while host-facing types remain separate."
    - "Validation helpers/builders return result objects and do not perform HTTP."
  artifacts:
    - path: "packages/boxnow-core/src/protocol.ts"
      provides: "Server-side BOX NOW protocol schemas and request builders"
      min_lines: 80
    - path: "packages/boxnow-core/src/protocol.test.ts"
      provides: "Protocol schema and builder tests"
      min_lines: 50
  key_links:
    - from: "packages/boxnow-core/src/protocol.ts"
      to: "packages/boxnow-core/src/primitives.ts"
      via: "compartment/country/shared primitive imports"
      pattern: "CompartmentSize"
    - from: "packages/boxnow-core/src/protocol.ts"
      to: ".planning/research/boxnow-official-api.md"
      via: "official API source hierarchy"
      pattern: "DeliveryRequest|Parcel|auth"
---

<objective>
Implement server protocol types and pure request-builder validation.

Purpose: Give the future Partner API Client typed, protocol-first shapes without adding HTTP behavior.
Output: Auth, location, delivery-request, parcel, and label schemas/types plus non-throwing validation helpers and tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/UBIQUITOUS_LANGUAGE.md
@.planning/public-api-sketch.md
@.planning/research/boxnow-official-api.md
@.planning/research/phase-02-external-library-audit.md
@.planning/phases/02-core-types-and-validation/02-CONTEXT.md
@.planning/phases/02-core-types-and-validation/02-RESEARCH.md
@.planning/phases/02-core-types-and-validation/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add protocol schemas and types</name>
  <files>packages/boxnow-core/src/protocol.ts, packages/boxnow-core/src/index.ts</files>
  <read_first>packages/boxnow-core/src/primitives.ts, packages/boxnow-core/src/locker.ts, .planning/research/boxnow-official-api.md, .planning/research/phase-02-external-library-audit.md</read_first>
  <action>Create `packages/boxnow-core/src/protocol.ts` with Valibot schemas and inferred types for OAuth auth session response, origin/destination protocol responses, delivery request input, delivery request response, parcel response, parcel label format, and label options. Use protocol-first field names for direct BOX NOW wire shapes. Keep API transport, token caching, fetch adapters, and live credentials out of this file. Export the public core symbols from `src/index.ts`.</action>
  <verify>C:\Users\SVall\.local\bin\rtk.exe rg -n "AuthSession|DeliveryRequest|Parcel|ParcelLabelFormat|Origin|Destination|access_token|parcels" packages/boxnow-core/src/protocol.ts packages/boxnow-core/src/index.ts</verify>
  <acceptance_criteria>
    - Auth response schema preserves protocol fields such as `access_token` where wire-facing.
    - Delivery request schema includes parcel item validation.
    - Parcel label format supports PDF and ZPL.
    - No `fetch`, `http`, or credential-loading behavior is added.
  </acceptance_criteria>
  <done>Server protocol contracts exist as pure schemas/types.</done>
</task>

<task type="auto">
  <name>Task 2: Add non-throwing request-builder validation helpers</name>
  <files>packages/boxnow-core/src/protocol.ts</files>
  <read_first>packages/boxnow-core/src/validation.ts, packages/boxnow-core/src/protocol.ts, .planning/phases/02-core-types-and-validation/02-CONTEXT.md</read_first>
  <action>Add focused pure helpers such as `validateDeliveryRequest`, `validateParcelLabelOptions`, and `validateAuthSessionResponse` that call `validateWithSchema` and return `BoxNowValidationResult<T>`. Do not add throwing aliases. Do not hide protocol-first wire field names behind domain aliases for direct request/response schemas.</action>
  <verify>C:\Users\SVall\.local\bin\rtk.exe rg -n "validateDeliveryRequest|validateParcelLabelOptions|validateAuthSessionResponse|BoxNowValidationResult" packages/boxnow-core/src/protocol.ts</verify>
  <acceptance_criteria>
    - Helpers return `BoxNowValidationResult`.
    - Helpers do not throw for expected invalid input.
    - Direct protocol schemas remain protocol-first.
  </acceptance_criteria>
  <done>Request builders and response validators are pure and non-throwing.</done>
</task>

<task type="auto">
  <name>Task 3: Add protocol validation tests</name>
  <files>packages/boxnow-core/src/protocol.test.ts</files>
  <read_first>packages/boxnow-core/src/protocol.ts, .planning/research/boxnow-official-api.md, .planning/research/phase-02-external-library-audit.md</read_first>
  <action>Add Vitest tests for valid/invalid auth response, delivery request input, parcel label options, and parcel response shapes. Include tests for the `voucher`/`parcel` distinction by asserting code names public protocol concepts around `Parcel`/`ParcelLabelFormat`, while the audit/glossary documents `Voucher` as ecosystem terminology. Tests must not require BOX NOW credentials.</action>
  <verify>pnpm exec vitest run packages/boxnow-core/src/protocol.test.ts</verify>
  <acceptance_criteria>
    - Valid protocol examples pass.
    - Invalid protocol examples return issues without throwing.
    - Tests run without network or credentials.
  </acceptance_criteria>
  <done>Protocol schemas and builders are covered by pure unit tests.</done>
</task>

</tasks>

<verification>
- [ ] `pnpm check`
- [ ] `pnpm test`
- [ ] `pnpm build`
</verification>

<success_criteria>
- Server-side protocol type and validation helpers exist without HTTP.
- Direct wire shapes use protocol-first names.
- Every pure helper added in this plan has tests.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-types-and-validation/02-04-SUMMARY.md`
</output>
